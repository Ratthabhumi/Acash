# E3.6 SESSION HANDOFF — ACASH V5

Canonical continuation point. A fresh OpenCode session may resume from this
document without relying on conversation memory.

Status of every tracked item below uses the explicit status set:
**DONE / PARTIAL / NOT STARTED / BLOCKED / UNKNOWN**.

---

## 1. Session Summary

ACASH V5 — E3.6 (Containerization, Paper Deployment Interlock & Execution
Infrastructure) implementation session. The human decision gate O1–O7 was
closed (all **Option A**), the ACASH-side E3.6 implementation was completed,
tested, and committed, and the session was paused at a clean canonical
checkpoint.

This document records: canonical git state, governance lock, human decisions,
per-workstream implementation status, verification results, artifacts, what
has NOT happened, HomeLab context, known risks, and the exact resume order.

**Nothing beyond the repository documentation was changed.** No deployment,
build, rating, or trading activity occurred.

---

## 2. Canonical Git State

| Item | Value | Status |
|---|---|---|
| Repository | `Ratthabhumi/Acash` (origin `https://github.com/Ratthabhumi/Acash.git`) | DONE |
| Baseline (finalized E3.6 audit/design/plan) | `6d3a909f6d01613bf084416e1adadb0cb2911bc9` | DONE |
| Latest implementation commit | `8a233cf032d48e2b4a9eb842700d7be90d186ed3` | DONE |
| Latest commit message | `feat(paper): implement E3.6 infra workstreams WS1/WS2/WS3/WS4/WS5/WS6/WS7/WS9 + O1-O7 decision record` | DONE |
| `HEAD` == `origin/main` at pause | `8a233cf032d48e2b4a9eb842700d7be90d186ed3` | DONE |

Canonical facts to preserve:

- `6d3a909` = finalized E3.6 audit/design/implementation-plan baseline.
- `8a233cf` = ACASH-side E3.6 implementation + Human Decisions.
- `E3.6-HUMAN-DECISIONS.md` exists and O1–O7 are explicitly approved.
- Implementation has NOT been deployed.
- No container has been built or deployed.
- No Paper Observation Window has been opened.
- Paper Authorization remains **NOT GRANTED**.
- Live remains **LOCKED**.

---

## 3. Governance Lock (HARD LOCK — immutable)

| Item | Status |
|---|---|
| D17-E (E3.5 observed sub-gate) | **HOLD** |
| MEC-0013 | **ARCHIVED / Option C** |
| HYP_003 | **NOT CREATED** |
| R1 | **NOT STARTED** |
| Backtest | **LOCKED / NOT AUTHORIZED** |
| Live Trading | **LOCKED** |
| ACASH canonical capital | **$0** |
| Paper Authorization | **NOT GRANTED** |

Critical ladder (never conflate):

```
INFRASTRUCTURE READY
!= STRATEGY QUALIFIED
!= PAPER AUTHORIZED
!= LIVE AUTHORIZED
```

E3.6 is **execution infrastructure only**. It is not and never implies strategy
qualification, Paper authorization, or trading authority.

---

## 4. Human Decisions O1–O7

All decisions: **OPTION A — APPROVED** by explicit human choice (recorded in
`E3.6-HUMAN-DECISIONS.md`, committed at `8a233cf`).

| ID | Decision | Approved posture |
|---|---|---|
| O1 | A | Operator-only `--resume`. No automatic feed recovery / automatic bar admission. Fail-closed on feed failure. |
| O2 | A | One session per UTC calendar day. Observation Window aggregates multiple sessions. |
| O3 | A | Evidence retention = observation window duration + 7-day margin. Capacity must still be proven by D5/G6. |
| O4 | A | Abort-all when deployment interlock sees an OPEN observation window. Automatic/CD compose deployment must abort non-zero before touching stack. |
| O5 | A | Ansible-managed systemd timer for backup scheduling. |
| O6 | A | Dedicated fixed container UID `10001:10001` with census guard. Collision = STOP and report; never silently choose another UID. |
| O7 | A | No numeric clock-skew budget during E3.6. Record host attestation + max observed skew. Any numeric threshold requires a later TEST REQUIRED measurement/decision. |

---

## 5. E3.6 Workstream Status

| WS | Workstream | Status | Implemented | NOT YET |
|---|---|---|---|---|
| WS1 | Containerization | **PARTIAL** | Dockerfile, `.dockerignore`, non-root UID 10001, `TZ=UTC`, `/data/docker/acash` layout | real digest-pinned production image; actual container build validation |
| WS2 | Persistent Storage | **PARTIAL** | design/code for single persistent root: `windows/`, `sessions/`, `logs/`, `review/` | real HomeLab persistence/recreation validation |
| WS3 | Runtime Provenance | **IMPLEMENTED APP-SIDE** | WindowManifest, runtime segments, transition-first records, provenance validation, fail-closed seal rules | — (HomeLab runtime evidence pending later phases) |
| WS4 | Deployment Interlock | **IMPLEMENTED APP-SIDE** | OPEN/SEALED/VOID state, OPEN deployment blocking, abort-all policy | real HomeLab/CD deployment drill |
| WS5 | Graceful Shutdown | **IMPLEMENTED** | bounded SIGTERM/SIGINT, second-signal fail-closed escalation, graceful stop/seal path | — (container-level signal delivery to verify in staging) |
| WS6 | Time Integrity | **IMPLEMENTED APP-SIDE** | segment open/close attestation, max observed skew recording, no fabricated close attestation | real HomeLab host NTP/time attestation validation |
| WS7 | Feed Reliability | **IMPLEMENTED APP-SIDE** | feed failure events, `halted_reason_chronology`, operator-only resume, fail-closed behavior | — (real-feed testing in staging) |
| WS8 | Resource Budget | **NOT COMPLETED** | — | real HomeLab measurement: CPU/RAM/swap/disk/I-O/network-feed latency/evidence growth; 24h/72h measurements; 3-month projection. **G6 must remain blocking until proven.** |
| WS9 | Observability | **APP-SIDE IMPLEMENTED** | operational Prometheus-text `/metrics` | real VictoriaMetrics/Grafana/Uptime Kuma integration validation |
| WS10 | Backup / Restore | **REPO IMPLEMENTED / LOCAL TESTED** | E3.6-WS10-BACKUP-RESTORE-CONTRACT.md, backup.py, restore.py, synthetic_acash_backup fixture, 14 unit tests, Ansible timer templates & tasks, auto_backup.sh flock/sha256/O3 retention | real HomeLab deployment (NOT VERIFIED), systemd timer activation (NOT APPLIED), real-host backup execution (NOT VERIFIED), real-host restore drill (NOT TESTED), off-host backup (NOT IMPLEMENTED) |

---

## 6. Test / Verification Status

Latest reported local verification (at `8a233cf`):

- Full pytest: **2197 passed, 12 skipped**.
- New E3.6 tests: **27 passed** (`tests/unit/paper/test_paper_e36_window.py` incl. window validation, transitions, interlock, graceful shutdown incl. second-signal escalation, metrics, attestation, persistence).
- MyPy `src/`: **clean** (225 source files).
- Remaining MyPy errors: **12 pre-existing baseline errors** in unmodified `tests/unit/paper/test_paper_e3_flight_recorder.py` — NOT introduced by E3.6, unchanged, out of scope.
- Secret scan: clean.
- `git diff --check`: clean before commit.
- Remote CI: **NOT AVAILABLE** (no CI configured).

**Scope boundary:** these results prove the current local ACASH implementation and
tests. They do NOT prove HomeLab runtime compatibility, which remains
**UNKNOWN** until later phases.

---

## 7. Files / Artifacts

Documentation (committed):

- `E3.6-AUDIT.md`
- `E3.6-DESIGN.md`
- `E3.6-IMPLEMENTATION-PLAN.md`
- `E3.6-HUMAN-DECISIONS.md`
- `E3.6-SESSION-HANDOFF.md` (this document)

Implementation artifacts (committed at `8a233cf`):

- `src/acash/paper/shutdown.py` — WS5 bounded graceful shutdown
- `src/acash/paper/window.py` — WS3/WS4/WS2 observation-window + interlock + manifest
- `src/acash/paper/metrics.py` — WS9 Prometheus-text `/metrics`
- `docker/Dockerfile` — WS1 container layout (non-root UID 10001)
- `docker/.dockerignore` — WS1 build-context hygiene
- related updated modules: `src/acash/paper/cli.py`, `src/acash/paper/__init__.py`
- `tests/unit/paper/test_paper_e36_window.py` — 27 E3.6 tests

Do not invent additional files.

---

## 8. What Has NOT Happened

- HomeLab has NOT been modified.
- Acer Ubuntu host has NOT been deployed to.
- Docker image has NOT been built on HomeLab.
- ACASH container has NOT been started.
- `docker compose` has NOT been run for ACASH.
- Ansible deployment has NOT been run for ACASH.
- Watchtower interaction has NOT been exercised.
- Backup/restore has NOT been exercised.
- 6h soak (amended from 72h via Human Ratification RATIF-E36-G7-SOAK-20260912) has NOT started.
- Paper Observation Window has NOT started.
- No broker/order API exists in this execution path.
- No real trading has occurred.

---

## 9. HomeLab Context

- HomeLab repo: `Ratthabhumi/Pi_Personal-Infrastructure`
- Host: `mew@homelab` — Ubuntu Server 24.04 LTS

Existing infrastructure to **REUSE** (do NOT duplicate):

- Docker Compose
- `homelab_proxy`
- Traefik
- Authelia
- Tailscale
- VictoriaMetrics
- Grafana
- Uptime Kuma
- Homepage
- Node Exporter
- cAdvisor
- existing Ansible/GitOps
- existing backup architecture
- existing `.env` secret contract

Do NOT create duplicate: proxy, auth, monitoring stack, TSDB, backup system,
scheduler, or network.

---

## 10. Known Risks

1. Watchtower can replace containers unless ACASH is explicitly excluded.
2. GitOps deployment can recreate the service unless interlock blocks it.
3. `--remove-orphans` is a provenance hazard if a skip-service strategy is used; **O4=A means abort-all**.
4. Runtime provenance must use ordered runtime segments.
5. Image digest alone does NOT establish runtime provenance.
6. Time skew has no numeric approved threshold yet (O7).
7. Resource capacity is **UNKNOWN** until measured on Acer (WS8/G6).
8. Backup/restore is not yet validated (WS10).
9. Same-disk backup remains a residual risk.
10. No Paper Authorization exists.

---

## 11. Exact Next Steps — Resume Order

Resume at home in this exact order. **Do not skip ahead.**

**PHASE A — Git / Repository Verification**
1. Pull/verify latest ACASH main.
2. Confirm `HEAD == 8a233cf032d48e2b4a9eb842700d7be90d186ed3`.
3. Confirm working tree clean (only pre-existing untracked `docs/research/` tolerated).
4. Read this handoff + `E3.6-IMPLEMENTATION-PLAN.md`.

**PHASE B — HomeLab Host Preflight** (SSH `mew@homelab`)
Verify: hostname, Ubuntu version, Docker version, Docker Compose version, CPU,
RAM, swap, `/data` capacity, `/ssd-data` capacity, Docker service, HomeLab repo
HEAD, time/NTP state, existing containers, network/egress.
**DO NOT deploy yet.**

**PHASE C — WS8 / G6**
Measure real resource capacity and evidence growth. Record results.
Do not assume 1TB storage is sufficient. G6 remains blocking until proven.

**PHASE D — WS10**
Implement/validate: evidence backup retention, Ansible systemd timer, restore
drill, evidence integrity after restore.

**PHASE E — ACASH Container Build** (only after required local gates)
Build image; verify base image digest, application commit identity, image
digest, non-root UID, no Docker socket, no published ports, storage mount.

**PHASE F — HomeLab Staging** (controlled integration only)
Exercise: `homelab_proxy`, Watchtower exclusion, deployment interlock,
persistent storage, monitoring, graceful shutdown, restart/recovery,
provenance, feed failure, time attestation.

**PHASE G — G1–G16**
Execute acceptance gates and collect evidence.

**PHASE H — 6h SOAK (AMENDED FROM 72h VIA HUMAN RATIFICATION RATIF-E36-G7-SOAK-20260912)**
Only after all prerequisite gates pass. **A soak is an acceptance gate, NOT
Paper Authorization.**

**PHASE I — HUMAN PAPER DECISION**
Only after successful review of staging/soak evidence. No automatic promotion.

---

## 12. Resume Checklist

Before any new work at home, confirm in order:

- [ ] `git fetch origin main`
- [ ] `HEAD == 8a233cf` and `origin/main` matches
- [ ] Working tree has no unintended changes (pre-existing `docs/research/` untracked file is tolerated — do not stage)
- [ ] This handoff + `E3.6-HUMAN-DECISIONS.md` + `E3.6-IMPLEMENTATION-PLAN.md` read
- [ ] Governance lock re-confirmed (D17-E HOLD; Paper/Live not authorized)
- [ ] No deployment/build/soak started without explicit human approval for that phase

---

## 13. Stop Conditions

Stop and ask the human before proceeding past these boundaries:

- Any request to start/deploy/restart ACASH containers, run `docker compose`,
  run Ansible on HomeLab, or open a Paper Observation Window.
- Any request to unlock Backtest, authorize Paper, authorize Live, create
  HYP_003, or start R1.
- Any modification to `docs/research/` or the research governance boundary.
- Any conflict between this handoff and the live repository state (verify first).
- Any numeric threshold (e.g. clock skew) that has not been approved per O7.

---

## 14. Final Governance Statement

E3.6 is execution infrastructure only. **INFRASTRUCTURE READY ≠ STRATEGY
QUALIFIED ≠ PAPER AUTHORIZED ≠ LIVE AUTHORIZED.** At pause:

- No build, no deployment, no soak, no Observation Window opened.
- Paper Authorization = **NOT GRANTED**. Live = **LOCKED**. Capital = **$0**.
- Resume point is deterministic: commit `8a233cf` + this document.