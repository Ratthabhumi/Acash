# E3.6 Human Decision Record — OPEN DECISIONS O1–O7

Status: **APPROVED (Option A for all seven)**
Authority: Human explicit approval via OpenCode decision gate (session, ACASH V5 — E3.6 Implementation Start).
Date: 2026-09-11 (UTC)
Baseline: `E3.6-AUDIT.md` + `E3.6-DESIGN.md` + `E3.6-IMPLEMENTATION-PLAN.md` @ commit `6d3a909`.

These are the only resolutions of `E3.6-DESIGN.md` §25 OPEN DECISIONS. They authorize
documented implementation *scope only*. **No strategy qualification, Paper authorization,
Live authorization, or trading authority is granted by or implied by this record.**

---

## O1 — Automatic feed reconnect (D6.3)

- **Decision:** **OPTION A — operator-only `--resume`; no automatic recovery.**
- Approved posture: fail-closed on disconnect/stale/failure; no automatic bar admission;
  recovery is explicit operator `--resume`/`reconnect()`. Window manifest enforces
  `auto_recovery_used: False`.
- Implementation impact: WS7 — keep existing halt/`FEED_DISCONNECTED` behavior; record
  `feed_failure_events` + `halted_reason_chronology`; no reconnect retry loop added.

## O2 — Calendar-day session boundary (D3.3)

- **Decision:** **OPTION A — one session per UTC calendar day.**
- Approved posture: Observation Window = aggregate evidence container; member sessions are
  one per UTC day; `--resume` stays within the current session.
- Implementation impact: WS3 — window supervisor starts a new session at UTC-day boundary;
  window aggregates sealed member sessions.

## O3 — Evidence retention band (D7.2)

- **Decision:** **OPTION A — observation-window duration + 7-day margin.**
- Approved posture: retention = window + 7 days; **storage capacity is NOT assumed
  sufficient** — must be verified via D5 measurement / G6 before window OPEN.
- Implementation impact: WS10 — evidence retention band extension + restore drill (G8);
  G6 capacity gate enforced before any QUIESCENT→OPEN.

## O4 — Deploy-pipeline behavior on OPEN interlock (D2.2)

- **Decision:** **OPTION A — abort-all.**
- Approved posture: when any window is `OPEN`, automatic/CD `compose up` ABORTS and returns
  non-zero before touching anything. No orphan-removal trap possible.
- Implementation impact: WS4 — interlock read step in the deploy pipeline early-aborts.
  Governed runtime transitions remain the only rotation path under OPEN (D2.5).

## O5 — Backup scheduling mechanism (D7.3)

- **Decision:** **OPTION A — Ansible-managed systemd timer.**
- Approved posture: evidence-band backup cadence is declarative in the HomeLab repo
  (survives reboot, GitOps-visible). Resolves R5 UNKNOWN without a new scheduler.
- Implementation impact: WS10 — HomeLab-side Ansible task; HomeLab change staged after
  ACASH-side local validation (deployment rule).

## O6 — Paper container UID (D10.2)

- **Decision:** **OPTION A — fixed `10001:10001`, census-guarded.**
- Approved posture: single non-root UID across the `/data/docker/acash/` mount root;
  Dockerfile `USER` + compose `user:` + Ansible chown-helper. **Census guard:** if a
  collision is found in the existing stack during WS1, STOP and report — no silent re-pick.
- Implementation impact: WS1/WS2 — UID plumbing + container write test (G14).

## O7 — Clock-skew budget (D4.1/O7)

- **Decision:** **OPTION A — no numeric budget adopted during E3.6; record attestation.**
- Approved posture: window manifest records host clock attestation (infra-supplied, D4.5) +
  `max_observed_skew_ms` at each runtime segment open/close. No rejection logic added;
  any numeric threshold is adopted only after D5/soak measurement via a later
  TEST REQUIRED gate. O7 remains structurally open, not silently locked.
- Implementation impact: WS6 — attestation capture + recording; no skew rejection.

---

## Governance lock (unchanged by this record)

D17-E HOLD · MEC-0013 ARCHIVED/Option C · HYP_003 NOT CREATED · R1 NOT STARTED ·
Backtest LOCKED · Live LOCKED · ACASH canonical capital $0 · Paper authorization
NOT GRANTED · no deployment performed · no container started.

**Signed:** Human (approved via interactive decision gate). OpenCode (recorded).